# Notarization values — copy to .env and fill in (release.sh reads it; .env is gitignored).
# One-time setup: xcrun notarytool store-credentials "$NOTARY_PROFILE" --apple-id "$APPLE_ID" --team-id "$TEAM_ID"
APPLE_ID=
TEAM_ID=
NOTARY_PROFILE=